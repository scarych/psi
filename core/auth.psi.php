<?php



return function ($params = array() ) {
    /*
    Для того, чтобы ввести корректную авторизацию, следует на вход подавать набор ключей для oauth.
    А также параметры пользовательских авторизаций с внутренними параметрами.

    Например
    PSI_Core::auth(
        'service_sessions'=>array(
            PSI::mysql()->service_users()
        )
    )
     */

}

?>